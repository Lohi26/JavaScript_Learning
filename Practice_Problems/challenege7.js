// Rewrite the 'calcAverageHumanAge' function from Challenge #2, but this time
// as an arrow function, and using chaining!
// Test data:
// § Data 1: [5, 2, 4, 1, 15, 8, 3]
// § Data 2: [16, 6, 10, 5, 6, 1, 4]


const sumAverage=(julia.map( ele => ele<=2? ele*2 : 16+(ele*4)).filter(arr => arr>=18).reduce( (acc,arrayEle,i,arr) => acc+arrayEle/arr.length,0));
console.log(sumAverage);


const kateMax=kate.reduce( (acc,ar) => Math.max(acc,ar),kate[0]);
console.log(kateMax);


//In previous challenge i have given thea answer in the chainng method🤷‍♀️